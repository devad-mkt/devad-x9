# <Project or Feature> External Review Packet

Packet date: <YYYY-MM-DD>
Purpose: <one bounded advisory question>

## Start here

1. Read <current context/router>.
2. Read <one detailed plan or contract>.
3. Read <one current receipt>.
4. Inspect <bounded offline ledger/export if needed>.
5. Return <one answer using the schema below>.

## Hard boundary

Everything is read-only except `<one exact output path>`. No repository,
database, provider, browser, ledger, Git, deploy, or production mutation is
authorized. Do not include secrets or overwrite another answer.

## Truth order

System/developer/user instructions and newest immutable Work Order -> current
Git/runtime -> accepted receipt -> live ledger -> router -> detailed plan ->
historical plans -> model opinions.

## Packet contents

| Path | Purpose | Evidence limit |
|---|---|---|
| <path> | <meaning> | <authority/advisory/history> |

## Required return

For each recommendation give `VERIFIED_ROUTE`, `DEFER_GUARD`, `REJECT_STALE`,
`REJECT_UNSUPPORTED`, or `OWNER_DECISION`, plus exact evidence, risk, smallest
change, proof, and one next action.

Review advice never grants execution authority.

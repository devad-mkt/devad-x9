# <Project or Feature> Work Order: <bounded outcome>

Status: `DRAFT / NO AUTHORITY UNTIL HASH_ACCEPTED`
Date/issuer/executor: <exact identities>
Parent amendment: <path/SHA-256 or NONE>

No chat text expands this file.

## Finish line

Problem: <evidence-backed problem>
Outcome/done token: <one observable whole-mission result and callback>
Expected envelope: `<90-240 min; 5-15 exclusive code files or one denominator>`
Manager task: <exact task identity>
Explicit non-outcomes: <what this mission does not claim>

## Preflight identity

Bind native worktree, branch, HEAD, upstream, GitDir/CommonDir, accepted base,
remote readback, known dirt, dependency receipt, and external resource identity.
Stop on drift.

## Frozen denominator

Name exact actors, states, controls, journeys, success/failure/recovery behavior,
and `REUSE/ADAPT/REIMPLEMENT/RETIRE` decisions.

## Claims and exclusions

| Exact path/resource | Allowed operation | Purpose |
|---|---|---|
| <claim> | <read/create/update> | <reason> |

List shared/serial-only resources and every prohibited live/destructive action.
An extra path requires a claim amendment.

## Queue and proof

1. truth lock and exact preflight;
2. bounded source/dependency trace;
3. implementation or readiness artifact;
4. focused and regression proof;
5. one stable review after bytes stabilize;
6. exact C1 source and C2 receipt, only when authorized;
7. security/pre-push gates, normal push/readback, remote equality and clean
   state, only when authorized;
8. manager wake callback.

Define exact tests, format/type/build, security/secret/tenant, browser/runtime,
ledger readback, rollback, and one structurally different safe fallback.

## Routine decision rights

Inside exact claims, allow current source/Git/tests/sibling-convention reads,
deterministic owned-output corrections, the smallest current-repo-compatible
internal arrangement, in-scope test/format repairs, one structural fallback,
and `PARTIAL/BLOCKED` on only the dependent claim while disjoint phases
continue.

Do not permit new dependencies/frameworks, shared claims, resource activation,
owner-scope expansion, or starting the proposed next mission.

## Wake callback

Immediately before final:

```text
<PROJECT>_LANE_WAKE
LANE: <lane>
STATE: DONE | BLOCKED
TOKEN_OR_BLOCKER: <exact>
BRANCH_HEAD: <branch>@<sha>
RECEIPT: <path>|SHA256:<hash>|BYTES:<n>
NEXT_GAP: <one dependency-ready mission proposal>
```

The callback wakes the manager and grants no next authority. If messaging is
unavailable, return the same packet as final.

## Stop and return

Stop on identity/hash/claim/resource/security drift. Preserve bytes.
Return exact changed paths, every gate as `PASS/FAIL/NOT_RUN/N/A`, C1/C2/remote
identities or `NONE`, external effects, blockers, rollback, and one next action.

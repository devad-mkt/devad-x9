interface:
  display_name: "Evidence to Implementation"
  short_description: "Build implementation-grade evidence contracts"
  default_prompt: "Use $evidence-to-implementation to trace every owner requirement through versioned evidence, settings, native owners, schemas, state machines, screenshots, tests, reviewer gates, and bounded Work Orders without leaving architectural decisions to the implementer."

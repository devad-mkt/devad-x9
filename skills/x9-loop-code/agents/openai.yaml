interface:
  display_name: "X9 Loop Code (Trial)"
  short_description: "Fresh-project legacy-controller trial only"
  default_prompt: "Use $x9-loop-code only for an owner-approved fresh disposable project canary. Keep $x9-loop-style as the default."

interface:
  display_name: "Telegram Codex Linker"
  short_description: "Resolve the installed skill scripts first, choose install or repair mode, recommend project roots with defaults, and keep user interruption minimal."
  default_prompt: "Use $telegram-codex-linker to resolve its installed script paths, choose whether this is install, repair, update, or rebind, recommend project scan roots with a default set, and then run the bundled bridge install script or the ctb maintenance path. Follow my language by default, tell me which language you picked, and only interrupt me for unavoidable external steps."

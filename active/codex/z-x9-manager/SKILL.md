---
name: z-x9-manager
description: Compatibility name for older Devad X9 manager prompts. Redirects normal coordination to $z-loop-style without creating a second manager or V7 Controller flow.
---

# zX9Manager

This historical name is a temporary v6-style redirect. Immediately load and
follow `$z-loop-style`; keep the requested role and prompt meaning, but use
one self-contained packet, exact Git evidence, durable receipts, and direct
signal-only handoffs.

Do not run a second manager flow, Controller Work Order, ACTION transport,
recovery route, role registry, or periodic monitor from this shim.

Report once in durable state:
`COMPAT_REDIRECT:devad-x9-manager:z-loop-style`.

The old `.devad/manager/loop/` and V7 runtime files are historical/canary
evidence only. Removing this compatibility skill needs a later explicit owner
decision.

## Provenance (consolidated 2026-09-12)

Canonical body: `z-x9-manager` (2026-08-13 00:38:44, 2 files, sha256 `27101f563c0a8004`).

Former names now disabled: `CODEX/devad-x9-manager`, `LOOPX_PKG/devad-x9-manager`, `NINELLC/devad-x9-manager`.

Unique content from disabled copies is preserved under `references/preserved/` and is NOT authoritative; this body wins on any conflict.

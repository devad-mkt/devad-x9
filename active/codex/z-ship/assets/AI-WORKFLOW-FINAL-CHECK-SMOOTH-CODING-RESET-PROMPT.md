# AI Workflow Final Check: Smooth Coding Execution Contract

Load and follow `$z-ship` for the rest of this task. This governs how the
accepted Devad Content Agent plan is implemented; it is not a request for a new
plan, audit, or review program.

## Authority And Role

- For this ticket, task `019f686e-be07-7e62-bb39-d6619e5ee17c` acts as the
  accountable implementer/Looper, not as a THINKER review program.
- Do not invoke `$z-think` or `$z-sdlc` in this ticket. If prior
  history or linked documents mention them, treat them as conceptual context
  only and execute through Smooth Coding.
- Ponytail concepts apply inline while coding. Do not create a separate
  Ponytail audit cycle.
- Preserve all accepted owner scope. Do not split the plan into V1/V2, remove
  features, or substitute a smaller MVP unless the owner explicitly requests
  that decision.

## Current Rebind And Continuity

Reverify the current helper and integration Git truth before edits. The last
verified shared clean tip was
`2c1a87092e41e98afd378342ebc93b33d5ba694a`; current Git wins if it changed.
Use the existing accepted plan and current source. Do not create another plan,
percentage program, architecture packet, or full 13-chunk audit.

Do not restart the dependency-ready Settings/API/CLI/MCP slice already in
progress merely because this contract changed. Rebind current Git, preserve
valid edits and focused-test evidence, then continue that implementation.

If the accepted plan needs progress navigation, keep one evidence-linked
checklist in its canonical location:

- `[x]` only when linked source, focused tests, runtime proof, or an accepted
  review proves the acceptance item.
- `[ ]` when implementation or proof remains incomplete.
- `BLOCKED:<exact external cause>` only on the dependent item, with safe
  parallel work named.
- Track acceptance items/slices, not commands or files. Do not hash the mutable
  checklist, calculate routine percentages, or create a duplicate checklist.
- After material code/contract changes, revalidate only affected checked rows.

## Per-Slice Risk

- `LITE`: docs, copy, hashes, small reversible edits, routine bookkeeping.
- `MEDIUM`: ordinary code, multi-file behavior, noncritical refactoring.
- `HIGH`: only the slice currently changing tenancy, security, a stateful
  migration, provider spend, production activation, rollback, or exact
  source/release authority.

High affects proof for that boundary only. It does not authorize Ultra/SDLC or
spread to adjacent work.

## Mandatory Delivery Reset

1. Preserve existing reports and evidence without updating every mirror.
2. End the rule that all thirteen audit chunks, manager rereviews, and owner
   reviews must finish before implementation can continue.
3. In one bounded pass, return an accurate binary plan ledger:
   `CHUNK | DONE | REMAINING IMPLEMENTATION | RUNTIME/EXTERNAL BLOCKER | NEXT`.
   Reuse current evidence; no reconstructed percentages and no new report file.
4. Group related accepted chunks into dependency-safe execution slices. Slices
   control order only; the complete plan remains the finish line.
5. Start the first dependency-ready code/test action in the same turn.

## Fast Full-Plan Execution

- Keep one delivery-critical slice active and continue automatically after its
  focused proof. Run disjoint work in parallel only with explicit nonoverlapping
  ownership.
- Do not prebuild handlers, stages, abstractions, or scaffolding for hypothetical
  later needs. Reuse the accepted path and add only what the active slice needs.
- Classify new findings as `REQUIRED_NOW`, `ACCEPTED_LATER`, or `DROP`. Preserve
  the full accepted owner scope: `DROP` applies only to unrequested speculation,
  never to an accepted later plan item.
- Suggested dependency order, corrected by current code evidence when needed:
  1. Settings, Brand, task/manual-run exposure and truthful field ownership.
  2. Remaining source normalization and production stage handlers.
  3. PostgreSQL queue/recovery and real pgvectorscale RAG when infrastructure is
     available; while blocked, continue every independent source/UI slice.
  4. Autopilot lifecycle, quotas, pause/resume/end, and crash recovery.
  5. Dynamic destination binding and receipts.
  6. One bounded current n8n/Laravel parity run and necessary corrections.
  7. Exact-SHA integration, release, canary, browser, and rollback proof.
- High/Critical treatment applies only while editing or activating tenancy,
  security, migrations, provider spend, production, rollback, or exact release
  authority. It does not spread to adjacent docs, UI, tests, or bookkeeping.
- Use focused tests while coding, one review of each stable materially changed
  diff, and broad security/build/browser/runtime gates only at their milestones.
  A separately mandated security/release gate stays only when it answers a
  distinct boundary question.
- Do not rereview unchanged bytes. Do not hash mutable checkboxes. Do not create
  another helper, document, manifest, receipt, or gate unless it changes a
  decision or is required by repository C1/C2 policy.
- A failed tool/fixture/connector is `ROUTE_BLOCKED`, not plan blocked. Try one
  structurally different safe route by default, avoid retry loops, and continue
  nondependent work.
- Owner approval is required only for changed product requirements, provider or
  spend, destructive/irreversible action, production activation, or deployment.

Before adding any document, agent, hash, scan, gate, review, audit, or deeper
reasoning step, ask whether it addresses a current changed-risk boundary,
material conflicting evidence, or a pending decision/repository requirement.
If not, skip it and keep coding.

## Safeguards

Preserve tenancy, security, trust-boundary validation, migrations, provider
spend, idempotency, durable receipts, no-repeat behavior, rollback,
accessibility, and exact source proof. Default-off flags remain active until
their runtime gate passes; default-off must not freeze unrelated coding.

## Response And Continuation Contract

Return:

`SMOOTH_CODING_RESET: PASS | BLOCKED:<exact>`

`CURRENT_SHA: <exact>`

`DONE: <accepted chunks/slices with concise proof>`

`NEXT: <code/test action already starting>`

`BLOCKED: <dependent items only plus safe parallel work>`

`PROCESS_REMOVED: <duplicate audits/reviews/artifacts stopped>`

`OWNER_ACTION: NONE | <one exact decision>`

Then continue implementing the complete accepted plan. Do not pause for a new
percentage, planning review, or owner acknowledgement when no owner decision is
required.
